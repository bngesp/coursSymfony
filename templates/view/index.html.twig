{% extends 'base.html.twig' %}

{% block title %}Hello ViewController!{% endblock %}

{% block stylesheets %}
    <style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    body{
        background-color: black;
        color: white;
    }
    </style>
{% endblock %}

{% block body %}


<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>
    <h2>
        mon nom est {{ nom }} je suis un eleve de la classe {{ classe }}
    </h2>
    Moi je mange seulement les fruits :
    <ul>
        {%  for element in tab %}
        <li>{{loop.index0}} - {{ element | upper }}</li>
        {% endfor%}
    </ul>
    {% set my_addres = "dakar plateau" %}
    i'm {{ p.prenom }} {{ p.nom }} i live in {{ my_addres }}
{# {{ dump(p) }}#}

</div>
{% endblock %}
